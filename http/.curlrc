# Mac OSX Google Chrome
user-agent = "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_12_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/61.0.3163.100 Safari/537.36"

# timeout
connect-timeout = 30

# リダイレクト時、自動的に前のURLをrefererとして設定
referer = ";auto"

# Show error message
show-error

# 破損したダウンロードを再開する
continue-at -

# トレースをcurl_trace.txtファイルに保存します。 curlコマンドを複数回実行するとこのファイルが上書きされることに注意してください
# trace curl_trace.txt

# curl_headers.txtファイルにヘッダー情報を格納します。 curlコマンドを複数回実行するとこのファイルが上書きされることに注意してください
# dump-header curl_headers.txt
